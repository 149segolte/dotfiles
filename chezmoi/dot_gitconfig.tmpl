{{- with (mustMerge ((includeTemplate "dynamic_data.yaml.tmpl" .) | fromYaml) .) -}}

[init]
    defaultBranch = main
[gpg]
    format = ssh
[gpg "ssh"]
    allowedSignersFile = {{ .chezmoi.homeDir }}/.ssh/git_allowed_signers
[commit]
    gpgsign = true
[tag]
    gpgSign = true
[user]
    name = 149segolte
    email = 37300847+149segolte@users.noreply.github.com
    signingkey = {{ .chezmoi.homeDir }}/.ssh/id_ed25519_sk.pub

{{- "\n" }}{{- end -}}
